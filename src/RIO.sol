// public intellectualproperty units

contract CostFunction {
    // "have, want" from caller's POV
    function cost(address have, address want, uint128 want_amount) returns (uint128 need_amount);
}
